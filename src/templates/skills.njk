<div class="skills">
    
    <h2>Skills</h2>

    <p>Some technologies I have used:</p>

    <div class="tags">


        {% for skill in skills %}

            {% if skill | is_string %}
                <div class="tag">{{ skill }}</div>
            {% else %}
                <div class="tag" title="{{ skill.title }}">{{ skill.text }}</div>
            {% endif %}

        {% endfor %}

    </div>

</div>

