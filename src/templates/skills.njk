<div class="skills">
    
    <h2>Skills</h2>

    <p>Some technologies I have used:</p>

    <div class="tags">

        {% for tag in skills %}
            
            {% if tag|is_string %}
                <div class="tag">{{ tag }}</div>
            {% else %}
                <div class="tag" title="{{ tag.title }}">
                    {{ tag.text }}
                </div>
            {% endif %}

        {% endfor %}


    </div>
    

</div>

